# infra/overlays/dev/mongo/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/sftp # Reference the generated base manifests

namespace: sftp # Ensure all resources are in the 'sftp' namespace
